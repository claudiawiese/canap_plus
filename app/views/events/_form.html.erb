<%= simple_form_for([@user, @event]) do |f| %>

  <%= f.input :game %>
  <%= f.input :date %>
  <%= f.input :description %>
  <%= f.input :address %>
  <%= f.input :capacity %>
  <%= f.input :fee %>

  <%= f.button :submit, "Add your event" %>
<% end %>
