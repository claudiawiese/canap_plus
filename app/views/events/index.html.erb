<h1>Events list </h1>
<%@events.each do |event|%>
<ul>
  <li><%= event.game %> <%= event.date%> <%= event.address %></li>
  <% end %>
</ul>

<div
  id="map"
  style="width: 100%;
  height: 600px;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>
